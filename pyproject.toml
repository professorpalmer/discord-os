[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "discord-os"
version = "0.5.17"
description = "Discord is the screen. This Mac is the computer: SQLite lineage, two jobs at once, Puppetmaster workers. Artifacts are Discord snowflakes plus sha256."
readme = "README.pypi.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "Discord OS contributors" }]
keywords = ["discord", "discord-os", "puppetmaster", "agent", "cli", "sqlite"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Communications :: Chat",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]
interactions = ["PyNaCl>=1.5"]

[project.scripts]
discord-os = "agent_discord.cli:main"
agent-discord = "agent_discord.cli:main"

[project.urls]
Homepage = "https://github.com/professorpalmer/discord-os"
Documentation = "https://github.com/professorpalmer/discord-os/blob/master/README.md"
Changelog = "https://github.com/professorpalmer/discord-os/blob/master/CHANGELOG.md"
Attribution-SaseQ = "https://github.com/SaseQ/discord-mcp"
Attribution-BrainDAO = "https://github.com/BrainDAO/mcp-discord"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
agent_discord = ["data/*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
