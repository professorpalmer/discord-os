# Cross-host status — read-only only (P2)

Multi-host is **status glance only** (dashboard / digest hosts list). No
multi-host brain-lake, no second JobPool, no silent local ssh cook. SSH gates
stay opt-in.
